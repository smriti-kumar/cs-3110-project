open Quizcaml.Flashcard_review

(* example flashcard set for testing purposes only: *)
(* let example = [("1 + 1", "2"); ("2 + 2", "4"); ("3 + 3", "6")] *)

let () = show_term ("What is 1 + 1?", "2")
let () = show_definition ("What is 1 + 1?", "2")