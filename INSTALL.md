To install and build our system:

Once you have our repository cloned/downloaded on your computer, cd into the root of the dune project.

Then run the following commands in terminal:\
opam install csv\
opam install mula\
opam install batteries\
opam install ounit2

Then, in the terminal, run the following commands in the given order: \
dune build\
dune exec quizcaml